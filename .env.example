# Copy to `.env` / `.env.local` for Vite (not used by Pages Functions).
#
# Set to `true` to require username/password before `/app` (needs AUTH_JWT_SECRET + D1 auth_users on the API).
# VITE_REQUIRE_AUTH=true
